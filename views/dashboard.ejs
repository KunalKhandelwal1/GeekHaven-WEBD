<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Title Page</title>
    <%- include("./partials/head") %>
    <style>
      
      .user-dashboard-card {
        background: #fff;
        color: #333;
        padding: 30px;
        border-radius: 16px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        width: 450px;
        transform: translate(110%,-5%);
        max-width: 90%;
        text-align: center;
      }

      .user-dashboard-card h2 {
        margin: 0;
        font-size: 1.6rem;
        font-weight: 600;
      }

      .user-dashboard-card .email {
        font-size: 0.95rem;
        color: #666;
        margin-bottom: 20px;
      }

      .stats {
        display: flex;
        justify-content: space-around;
        margin-top: 15px;
        gap: 15px;
      }

      /* Each stat box */
      .stat-box {
        flex: 1;
        background: #f8f9fc;
        border-radius: 12px;
        padding: 15px;
        text-align: center;
        transition: all 0.3s ease;
      }

      .stat-box:hover {
        background: #e9efff;
      }

      .stat-box h3 {
        font-size: 1.2rem;
        color: #007bff;
        margin-bottom: 5px;
      }

      .stat-box p {
        font-size: 0.9rem;
        color: #444;
      }
    </style>
  </head>
  <body>
    <%- include("./partials/nav") %>
    <div class="user-dashboard-card">
      <h2><%= user ? user.name : "Guest" %></h2>
      <p class="email"><%= user ? user.email : "Not logged in" %></p>
      <div class="stats">
        <div class="stat-box">
          <h3>Bookmarked</h3>
          <p>Number of Q bookmarked</p>
        </div>
        <div class="stat-box">
          <h3>Number of Q solved</h3>
          <p>Progress details</p>
        </div>
      </div>
    </div>
    <%- include("./partials/script") %>
  </body>
</html>
